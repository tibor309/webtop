# 🖥️ Kubuntu

[Kubuntu](https://kubuntu.org/) is an official flavor of the Ubuntu operating system that uses the KDE Plasma Desktop Environment instead of the GNOME desktop environment.

## Setup

To use this desktop, change the docker image tag to `kubuntu`. For setup options and configuration, check the [documentation](https://github.com/tibynx/webtop?tab=readme-ov-file#setup).
